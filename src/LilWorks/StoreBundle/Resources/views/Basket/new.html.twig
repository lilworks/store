{% extends 'base.html.twig' %}

{% block body %}
    <h1>
        {% trans %}brand new{% endtrans %}
    </h1>

    <div class="lilworks_list_showAction">
        <ul class="list-inline">
            <li class="list-inline-item">
                <a href="{{ path('brand_index') }}" class="btn btn-info btn-sm">
                    <i class="fa fa-list"></i> {% trans %}brand list{% endtrans %}
                </a>
            </li>
        </ul>
    </div>

    <div class="row">
        <div class="row-sm-10 offset-2">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" class="btn  btn-primary" value="{% trans %}create{% endtrans %}" />
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
