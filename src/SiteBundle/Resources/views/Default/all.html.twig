{% extends 'base2col.html.twig' %}

{% block body %}
    {% block stylsheets %}
        {{ parent() }}
        <style>
            .price-range-disabled{
                visibility: hidden;
                height: 0;
            }
        </style>
    {% endblock %}
    {% block javascripts %}
        {{ parent() }}

        <script>
            $( document ).ready(function() {

                /*
                 console.log($('.price-range-left'));
                 console.log($('.price-range-left').slider());

                 console.log("right slider PASS");
                 $(".price-range-left").slider({});
                 console.log("STOP");
                 */
                $(".selectpicker").selectpicker();
                $(".price-range").slider({
                    formatter: function(value) {
                        return  value + "€";
                    }
                });
            });

        </script>

    {% endblock %}




    <div class="col-md-3">
        <div class="card">
            <div class="card-header">
                {% trans %}sitebundle.filter{% endtrans %}
            </div>
            <div class="card-block">
                <p class="card-text">
                <form method="get"  class="formFilter">
                    {{ form_rest(formFilter) }} <input type="submit" class="btn btn-sm btn-primary" name="submit-filter" value=" {% trans %}sitebundle.button.filter{% endtrans %}" />
                </form>
                </p>
                <a href="#" class="btn btn-primary">Go somewhere</a>
            </div>
        </div>

    </div>
    <!-- /.col-lg-3 -->
    <div class="col-md-9">


        <div class="card">
            <div class="card-header">

                <div class="row">
                    <div class="col-md-6">
                        <h3>

                        </h3>
                    </div>
                    <div class="col-md-6" >

                    </div>
                </div>





            </div>
            <div class="card-block">
                <div class="card-text">



                    {% if pagination is defined %}
                        <div class="pagination">
                            {{ knp_pagination_render(pagination) }}


                            <span class="alert alert-info" role="alert" >
                <i class="fa fa-sort" aria-hidden="true"></i>
                                {{ knp_pagination_sortable(pagination, 'priceOnline', 'p.priceOnline') }}
            </span>
                        </div>
                    {% endif %}

                    <div class="row">



                        {% for product in pagination %}
                            {{ productService.getProduct(product.id)|raw }}
                        {% endfor %}


                    </div>


                    {% if pagination is defined %}
                        <div class="pagination">
                            {{ knp_pagination_render(pagination) }}
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>


    </div>






{% endblock %}
